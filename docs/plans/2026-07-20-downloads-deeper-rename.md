# Future deeper Downloads rename

v0.1.3 performs a user-facing Jobs → Downloads rename only. The REST `/jobs` contract, `Job` model, and `jobs` table remain stable for compatibility. A later major/minor migration can rename database/API internals after client compatibility is planned.
